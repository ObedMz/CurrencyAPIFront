import { LayoutGrid } from "lucide-react";

export default function Header() {
    return(
        <header className="h-[50px] p-3 flex gap-1">
            <LayoutGrid /> Currency Converter API
        </header>
    )
}